//Variables : es na manera que se usa para grabar un dato y luego poder ser utilizado despues 
//Strings : son textos que van entre comillas "texto"
//Funciones (argumentos, return) :son un conjunto de intrucciones que realizan una tarea
//Declaraciones if : es un condicional que se usa para definir si es algo falso o verdadero y poder continuar con un proceso 
//Valores booleanos (true, false) :son valores que se usan para afirmar si algo es verdadero (se activa) o falso (no se activa).
